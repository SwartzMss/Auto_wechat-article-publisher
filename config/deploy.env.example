# Minimal deploy overrides (copy to config/deploy.env and fill).
# Omitted values fall back to sensible defaults inside scripts/deploy.sh.

# --- Required ---
DOMAIN=example.com
SSL_CERT_PATH=/etc/ssl/certs/example.crt
SSL_KEY_PATH=/etc/ssl/private/example.key

# --- Commonly changed ---
# HTTP/HTTPS ports (frontend redirect + TLS)
HTTP_PORT=80
HTTPS_PORT=443
# Backend listen address; empty = use config/config.json server_addr
BIND_ADDR=:8080
# Extra hostnames (space-separated), optional
DOMAIN_ALIASES=www.example.com
